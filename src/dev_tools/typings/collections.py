from __future__ import annotations

from dev_tools.typings.strings import (
    DirectoryName,
    FileExtension,
    FileName,
    RelativePathString,
)

DirectoryNames = tuple[DirectoryName, ...]
FileNames = tuple[FileName, ...]
FileExtensions = tuple[FileExtension, ...]
RelativePathStrings = tuple[RelativePathString, ...]
